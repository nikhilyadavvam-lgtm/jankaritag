<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Update QR Info</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
  <div class="container mt-5">
    <h2 class="mb-4">Update QR Info</h2>

    <% if (locals.data) { %>

   <p>Old Information </p>
          <div class="space-y-3 text-sm sm:text-base">
          <p><span class="font-semibold">Category:</span> <%= data.category %></p>
          <p><span class="font-semibold">Custom ID:</span> <%= data.customId %></p>
          <p><span class="font-semibold">Name:</span> <%= data.name %></p>
          <p><span class="font-semibold">Location:</span> <%= data.location %></p>
          <p><span class="font-semibold">Phone:</span> <%= data.phone %></p>
          <p><span class="font-semibold">Info:</span> <%= data.info %></p>
          
        </div>

      <form action="/qrinfo/update/<%= data._id %>" method="POST" class="card p-4 shadow-sm">

        <!-- Category -->
        <div class="mb-3">
          <label for="category" class="form-label">Category</label>
          <select id="category" name="category" class="form-select">
            <option value="VEHICLE" <%= data.category === 'VEHICLE' ? 'selected' : '' %>>Vehicle</option>
            <option value="WATERTANK" <%= data.category === 'WATERTANK' ? 'selected' : '' %>>Water Tank</option>
            <option value="OTHERS" <%= data.category === 'OTHERS' ? 'selected' : '' %>>Others</option>
          </select>
        </div>



        <!-- Name -->
        <div class="mb-3">
          <label for="name" class="form-label">Name</label>
          <input type="text" id="name" name="name" class="form-control" value="<%= data.name %>">
        </div>

        <!-- Location -->
        <div class="mb-3">
          <label for="location" class="form-label">Location</label>
          <input type="text" id="location" name="location" class="form-control" value="<%= data.location %>" required>
        </div>

        <!-- Phone -->
        <div class="mb-3">
          <label for="phone" class="form-label">Phone</label>
          <input type="text" id="phone" name="phone" class="form-control" value="<%= data.phone %>">
        </div>

        <!-- Info -->
        <div class="mb-3">
          <label for="info" class="form-label">Info</label>
          <textarea id="info" name="info" class="form-control"><%= data.info %></textarea>
        </div>

  
      
        <!-- Submit -->
        <button type="submit" class="btn btn-success">Update Info</button>
      </form>
    <% } else { %>
      <p class="text-danger">No data found to update.</p>
    <% } %>
  </div>
</body>
</html>